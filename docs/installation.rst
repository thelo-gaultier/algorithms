.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install algorithms

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv algorithms
    $ pip install algorithms
